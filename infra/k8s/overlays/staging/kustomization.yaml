apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: devworkflow-staging

resources:
  - ../../base

commonLabels:
  environment: staging

patchesStrategicMerge:
  - deployment-patch.yaml